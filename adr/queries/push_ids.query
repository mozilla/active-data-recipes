from: repo
select:
    - push.id
where:
    and:
        - in: {branch.name: ["autoland", "mozilla-inbound"]}
        - gte: [push.date, {date: "%(from_date)s"}]
        - lte: [push.date, {date: "%(to_date)s"}]
limit: 100000
