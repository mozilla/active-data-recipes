from: repo
groupby:
    - push.user
where:
    and:
        - eq: {branch.name: "%(branch)s"}
        - gte: [push.date, {date: "%(from_date)s"}]
        - lte: [push.date, {date: "%(to_date)s"}]
limit: 1000
